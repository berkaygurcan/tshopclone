import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  itemImage: {
    width: 60,
    height: 100,
    borderRadius: 20,
    borderWidth: 0.2,
  },
  inputNameText: {
    width: 70,
  },
  container: {gap: 8},
});

export default styles;
